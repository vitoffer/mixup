services:
  nginx:
    build: "./nginx"
    container_name: mixup-nginx
    volumes:
      - ./certs:/etc/nginx/certs
      - ./frontend/dist:/etc/nginx/html
    networks:
      - mixup-net
    ports:
      - "80:80"
      - "443:443"

networks:
  mixup-net:
